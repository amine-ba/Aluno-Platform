import React from "react";
import { Sidebar, SequenceItem, RecommendedPath, Timetable, Badges } from "@components";

const Dashboard: React.FC = () => {
  return (
    <div>
        <RecommendedPath/>
    </div>
  );
};

export default Dashboard;